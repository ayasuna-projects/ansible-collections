- name: "Local"
  connection: "local"
  hosts: "localhost"
  serial: 1
  gather_facts: true
  tasks:
    - name: "[Local] Including 'local.yml' tasks from role '{{ ayasuna__utilities__ingot__privates__build_arguments.prepared.build_ansible_role_fqn }}'"
      ansible.builtin.include_role:
        name: "{{ ayasuna__utilities__ingot__privates__build_arguments.prepared.build_ansible_role_fqn }}"
        tasks_from: "local.yml"